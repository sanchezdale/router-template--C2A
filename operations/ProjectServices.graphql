query ProjectServices($projectId: ID!) {
  project(id: $projectId) {
    id
    name
    services {
      edges {
        node {
          id
          name
          updatedAt
          serviceInstances {
            edges { node { id environmentId region upstreamUrl } }
          }
        }
      }
    }
  }
}

